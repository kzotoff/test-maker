* very simple quiz and presentation building app
* no auth, no vulnerability protection
* no legacy support, modern JS syntax and CSS styles
* just attach it to your webserver or run "php -S localhost:4000" at root folder